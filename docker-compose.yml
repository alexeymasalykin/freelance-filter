services:
  freelance-filter:
    build: .
    container_name: freelance-filter
    restart: unless-stopped
    user: "${UID:-1000}:${GID:-1000}"
    env_file: .env
    volumes:
      - ./data:/app/data
    stdin_open: true
    tty: true
